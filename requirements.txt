# ── 핵심 데이터 처리 ─────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0

# ── 주가 데이터 수집 ─────────────────────────────────────────
yfinance>=0.2.36
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── 뉴스/RSS ──────────────────────────────────────────────────
feedparser>=6.0.10

# ── 유틸리티 ──────────────────────────────────────────────────
lxml>=4.9.0

# ── (선택) 무료 로컬 뉴스감정 — FinBERT(영어) + KR-FinBert-SC(한국어) ─────────
#   API 키 불필요. transformers 설치 시 confidence_engine 이 로컬 추론을 자동 사용
#   (미국·한국 모두). 미설치 시 키워드 분석으로 자동 폴백.
#   ⚠️ torch 는 용량이 커서 Vercel 서버리스에는 부적합 — 로컬/자체호스팅에서만 설치.
#   설치:  pip install "transformers>=4.36.0" "torch>=2.0.0"
# transformers>=4.36.0
# torch>=2.0.0

# ── market_briefing 모듈 의존성 ──────────────────────────────
# (requests, bs4, yfinance, lxml 은 위에서 이미 선언됨)
# Vercel Python 런타임: certifi 는 자동 포함되나 명시 권장
certifi>=2024.2.2
